//define the matrix entity
export interface IMatrix {
    matrixId: number;
    matrixName: string;
    matrixCode: string;
    joinedDate: string;
    description: string;
    imageUrl: string;
    price: number;
}